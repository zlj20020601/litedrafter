# C_eff 归因实验设计：per-group ledger 与因果控制（2026-08-17）

> **做了什么：** 复盘 0812/0816 两轮实验的因果链证据强度：删除错误结论（"25.75→9 = 2.86x 碎片化损失"）、将未证明机制降级为假设清单（H1-H4）、标注 AR waiting 的 token-budget 混杂变量，并制定下一步归因实验计划（E0-E4）——核心是 per-group KV 账本插桩，钉死"DFlash 第 10 个请求被哪个 group 挡住"。

## 因果链证据评级（0817 裁定）

| 因果链 | 当前状态 | 我的判断 |
| --- | --- | --- |
| ① DFlash → 4→38 groups | 已观测 | **强证据** |
| ② DFlash active capacity 很低 | 已观测 | **强证据** |
| ② KV saturation → running plateau | 9 active + 97.5% KV | **非常强** |
| ③ plateau → waiting → TTFT 暴增 | 已观测 | **非常强** |
| ③ plateau → throughput gain 消失 | 已观测 | **强证据** |
| ④ 38 groups → capacity=9 | **未证明** | 还缺 causal control |
| "25.75→9 是 2.86x 碎片化损失" | 错误 | **删除** |
| "FA page 是 binding constraint" | 未证明 | **需要 per-group 插桩** |

## 已执行修订（同步至 0812/0816 笔记）

1. **删除 2.86x 表述**（0816 面试口径、做了什么、对比节）：分子 25.75 口径不成立——它假设每请求 KV 需求随长度线性缩小，而 mamba state 是常数、SW 按窗口、drafter FA 页对齐 block ≈600 tok/block。不作为 baseline。
2. **"FA 页填充是 binding constraint"降级为 H2 假设**（两口径结论相反，见下）。
3. **0812 L3.5 勘误**：旧版 AR 手算"259 blocks/请求、池 15,799"为单位混用 + 循环论证（15,799×17.2MB≈271GB 物理不可能）。修正版：11 blocks/请求、池 676 × 17.2MB ≈ 10.82GiB ✓，同时解开 0816 踩坑5（676 = 池总 group-blocks）。
4. **④ 保持未证明**，待 causal control（E3/E4）。

## 混杂变量：scheduler token budget（0817 新标注）

- `max_num_batched_tokens=16384`；C≥17 时 C×1024 prefill 超预算 → chunked prefill 分批 → AR 的 waiting 混入 token-budget 瞬时排队。与观测吻合：C=16 恰好 16×1024=16384 不触发、waiting=0；C=20 起 waiting=2→62 而 running 始终=C、kv 远未满。
- vLLM 0.26.0 官方说明：speculative decoding 下实际 `max_num_scheduled_tokens` 可能比 max_num_batched_tokens 更小（drafter 向 batch 加入额外 token slots）。
- **处置**：AR 高 C 段 waiting 不与 DFlash 直接对读；后续每点记录 resolved max_num_scheduled_tokens（AR/DFlash 都记）。
- DFlash 侧干净：prefill 9×1024=9,216 ≪ 16,384；verify ~9×16=144 tok/iter 也不触顶 → DFlash waiting 基本纯 KV admission。

## 四个数卡死任何简单模型

| | 满长口径(4096) | 实长口径(1280) | 实测 C_eff |
|---|---|---|---|
| AR | 61（源码） | 195（÷1280） | >96 未饱和 ✓ 两口径都不违反 |
| DFlash | 8.05（源码） | 25.75（÷1280，已废） | **9 ← 紧贴满长口径** |

- **推论 1**：AR 实测 >96 > 启动 61 → 运行时 admission 按实际长度，不做满长预留。C=96 kv=0.86 反解 ≈104MB/请求，介于 1280 朴素需求（~94MB）与满长 183MB 之间且靠近前者 ✓。
- **推论 2**：DFlash 实测 9 ≈ 8.05、远离 25.75 → 运行时预留接近满长最坏。kv=0.975 反解 ≈0.975×9.65GB÷9 ≈ **1.05GB/请求**，接近 4096 最坏的 1.2GB；而 1280 朴素需求仅 ~0.2-0.3GB → **放大 ~4-6x**。
- 同一 scheduler、同一 KVCacheManager，两种模式预留行为不对称 → DFlash 路径存在专属放大机制 = H1-H4。（反解数字依赖 kv_usage 口径假设，精确值以 ledger 为准）

## 假设清单（全部未证明）

| # | 假设 | 一句话 |
|---|---|---|
| H1 | per-group 预留/watermark 放大 | 运行时新块预留按组进行，38 组 vs 4 组，组数 ×9.5 放大 |
| H2 | 页对齐 block 当量放大 | drafter FA block_size = page÷per_token ≈ 600 tok/block，预留粒度暴涨 |
| H3 | SD 专属预留 | num_speculative_tokens=15 被乘进 FA 层 block 需求 |
| H4 | mamba 常数 state 主导 | 24 组 × 常数 state 页，不随长度缩（用户主推方向） |

**0818 终审（E0+E1+E3 后）**:H1 死亡（watermark=0）；H2 存在但非 binding（runtime 592 tok/block，attn 仅 42 块/请求）；H3 存在但 592 粒度下不可见（精确边界才跳块）；**H4 为最终主因并量化：mamba 384 块/请求=准入需求 93%、931MB/请求，第 10 个请求被它挡住**——用户最初主推方向正确。

## FA vs Mamba：两种口径结论相反（不可预设 binding group）

1280 workload 每请求粗算（占位值，精确值待 spec dump）：

| | 字节口径 | 池块口径 |
|---|---|---|
| mamba (24组) | ≈96MB ← 大头 | 48 blocks ← 小头 |
| target FA (8组) | ≈42MB | 640 blocks ← 大头（13x） |
| 结论指向 | H4 | H2 |

**哪个口径决定 admission 未知 → ledger 是唯一裁决。** 已源码核实：mamba 每组 2 blocks = 1 + cdiv(15, 2048)（长度无关常数）；SW 窗口大小待 drafter config.json 确认。

## DFlash per-req 502 blocks 之谜（ledger 第一目标）

由启动日志反解：4039 blocks ÷ 8.05x ≈ **502 blocks ≈ 1.2GB/请求**（4096 满长）。但按 AR 同法朴素估算（mamba 48页×2.39MB + FA 8×256×64KB + drafter ≈ 0.28GB ≈ 117 blocks → 34.5x）与 8.05 不符，**缺口 ~4.3x 未解释**。需要 startup spec dump：各 spec 的 page_size_bytes / page_size_padded / block_size / SW window / num_speculative_blocks 精确值，逐项分解 502。

## 实验计划 E0-E4（便宜优先，无卡/有卡分类）

| # | 资源 | 内容 | 产出 |
|---|---|---|---|
| E0 | 无卡 | ✅ 0818 完成：稳态重析，AR 无稳态排队、DF ceiling=9 铁证 | [[E0_E1_归因执行记录_20260818]] |
| E1 | 无卡 | ✅ 0818 完成：源码侦察 + 离线 ledger（H2 结论后被 E3 修正） | 同上 |
| E2 | 有卡 | ◐ DF 侧已被 E3 顺带完成（C=8/9/10，knee=10）；**AR 侧 C=104-128 未做**（找 AR 真实 knee，~40min GPU） | 补点后的完整扫描 |
| E3 | 有卡 | ✅ 0818 完成：现场 ledger，④ 闭合，binding=mamba(93%)，426 块/请求零方差 | [[E3_现场Ledger执行记录_20260818]] |
| E4 | 可选,有卡 | ⬜ 未做：max_model_len=1280 启动 DF，分解常数项/长度项（④ 已闭合，优先级降低） | 常数项/长度项分解 |

每点同时记录 resolved max_num_scheduled_tokens（AR/DFlash）。

## 验收标准

- **E3 完成** = 能指着一个 group 说"C=10 的第 10 个请求因该组 free blocks 不足被拒" → ④ 闭合
- **E0 完成** = AR waiting 的 token-budget 成分与 KV admission 成分可分解
- **E4 完成** = 常数项/长度项分解获得第二个独立口径

## 术语裁定（0817，简历/汇报口径）

- 现在可写：**active concurrency ceiling = 9**（A_max 实测，running 钉死 9）
- ~~不可写：queueing knee = 具体值~~ **0818 E3 已解锁：C=9→run=9/wait=0 且 C=10→run=9/wait=1 → 可写：queueing knee = 10**（0817 预设的裁定条件逐字满足）
- E2 补完后若 C=9→run=9, wait=0 且 C=10→run=9, wait=1，则可写：**active concurrency ceiling=9，queueing knee=10**（✅ 已于 0818 E3 满足）
- 产出措辞用"**识别** grouping 退化与高并发容量瓶颈"，不用"证明 grouping 退化即最终根因"——~~38 groups → A_max=9 的因果闭合需 allocate_slots() / 分组修改控制实验（E3/E4）~~ **0818 E3 ledger 已闭合：第 10 个请求准入需求 412 块中 mamba 占 384(93%)，被 24 个单层 GDN 组挡住**
- 已强证明可继续写：KV saturation → active plateau → capacity waiting → TTFT↑ → throughput plateau（观测链）+ 4→38 groups（观测）+ per-group ledger 因果实锤（0818）

## 关联

- [[三层容量实验_Qwen3.5-4B_20260812]]（源码公式 + 0817 勘误）
- [[阶段二_C_eff拐点扫描_20260816]]（C_eff 数据 + 0817 修订标注）
